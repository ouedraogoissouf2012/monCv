[project]
name = "moncv-release-tools"
version = "0.1.0"
description = "Repository-owned local release and browser verification tools"
requires-python = "==3.12.*"
dependencies = [
  "playwright>=1.55,<2",
]

[tool.uv]
package = false
