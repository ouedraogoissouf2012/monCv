// Tests Flutter — voir les sous-dossiers pour les tests détaillés :
// - test/providers/ : tests unitaires des providers (AuthProvider, CvProvider)
// - test/widgets/  : tests de widgets des écrans principaux (LoginScreen, HomeScreen)

void main() {
  // Ce fichier est un point d'entrée vide.
  // Les tests sont organisés dans les sous-dossiers ci-dessus.
}
